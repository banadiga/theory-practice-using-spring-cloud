spring:
  application:
    name: Eureka

server:
  port: 8762

eureka:
  instance:
    preferIpAddress: false
    metadataMap:
       zone: zone2
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
